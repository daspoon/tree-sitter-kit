module.exports = grammar({
    name: 'FunLang',
    rules: {
        Block: $ => seq(optional($.DefSequenceDelimitedBySemicolon), $.Expr),
        Decl_enum: $ => $.Enum,
        Decl_fun: $ => $.Fun,
        Decl_let: $ => $.Let,
        Decl_typedef: $ => $.Typedef,
        Def: $ => choice($.Decl_enum, $.Decl_fun, $.Decl_let, $.Decl_typedef),
        DefSequenceDelimitedBySemicolon: $ => seq($.Def, ';', repeat(seq($.Def, ';'))),
        Enum: $ => seq('enum', $.Name, '{', $.EnumCaseSequenceSeparatedByComma, '}'),
        EnumCase: $ => seq($.Name, optional($.ParamArray)),
        EnumCaseSequenceSeparatedByComma: $ => seq($.EnumCase, repeat(seq(',', $.EnumCase))),
        Expr: $ => choice($.Expr_add, $.Expr_and, $.Expr_apply, $.Expr_block, $.Expr_eql, $.Expr_lambda, $.Expr_match, $.Expr_mu, $.Expr_mul, $.Expr_name, $.Expr_neg, $.Expr_numb, $.Expr_or, $.Expr_pow, $.Expr_project, $.Expr_tuple),
        ExprArray: $ => seq('(', optional(seq($.Expr, repeat(seq(',', $.Expr)))), ')'),
        Expr_add: $ => prec.left(4, seq($.Expr, choice('+', '-'), $.Expr)),
        Expr_and: $ => prec.left(3, seq($.Expr, choice('&&'), $.Expr)),
        Expr_apply: $ => prec.left(9, seq($.Expr, $.ExprArray)),
        Expr_block: $ => seq('{', $.Block, '}'),
        Expr_eql: $ => prec.left(1, seq($.Expr, choice('=='), $.Expr)),
        Expr_lambda: $ => seq('!', $.ParamArray, '->', $.Type, '.', $.Expr),
        Expr_match: $ => seq('match', $.Expr, '{', $.MatchCaseSequenceSeparatedByComma, '}'),
        Expr_mu: $ => seq('!', $.Name, $.ParamArray, '->', $.Type, '.', $.Expr),
        Expr_mul: $ => prec.left(5, seq($.Expr, choice('*', '/', '%'), $.Expr)),
        Expr_name: $ => $.Name,
        Expr_neg: $ => prec(7, seq(choice('-'), $.Expr)),
        Expr_numb: $ => $.Int,
        Expr_or: $ => prec.left(2, seq($.Expr, choice('||'), $.Expr)),
        Expr_pow: $ => prec.right(6, seq($.Expr, choice('^'), $.Expr)),
        Expr_project: $ => prec.left(8, seq($.Expr, '.', $.Int)),
        Expr_tuple: $ => $.ExprArray,
        Fun: $ => seq('fun', $.Name, $.ParamArray, '->', $.Type, '{', $.Expr, '}'),
        Int: $ => /[0-9]+/,
        Let: $ => seq('let', $.Param, '=', $.Expr),
        MatchCase: $ => seq($.Name, $.NameArray, '=>', $.Expr),
        MatchCaseSequenceSeparatedByComma: $ => seq($.MatchCase, repeat(seq(',', $.MatchCase))),
        Name: $ => /[a-zA-Z_]+[0-9a-zA-Z_]*/,
        NameArray: $ => seq('(', optional(seq($.Name, repeat(seq(',', $.Name)))), ')'),
        Param: $ => seq($.Name, ':', $.Type),
        ParamArray: $ => seq('(', optional(seq($.Param, repeat(seq(',', $.Param)))), ')'),
        Type: $ => choice($.Type_const, $.Type_func, $.Type_name, $.Type_tuple),
        TypeArray: $ => seq('(', optional(seq($.Type, repeat(seq(',', $.Type)))), ')'),
        Type_const: $ => prec(1, seq($.Name, $.TypeArray)),
        Type_func: $ => prec.right(1, seq($.Type, '->', $.Type)),
        Type_name: $ => $.Name,
        Type_tuple: $ => $.TypeArray,
        Typedef: $ => seq('typedef', $.Name, '=', $.Type)
    }
})
